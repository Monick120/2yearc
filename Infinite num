#include <stdio.h>
void main()
{
    int a;
    int sum=0;
    while(1)
    {
        scanf("%d,&a");
        if(a==0)
        {
            break;
        }
        sum+=a;
    }
    printf("Sum of the inputs is %d",sum);
}
